// Strings more than one view shows, so they read the same everywhere.

/** Why Start is disabled when every setting is valid: the run has no name. */
export const NAME_THE_SAMPLE = "Name the sample to start.";
