{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "default",
  "description": "The main window may call the shell's own commands and the core APIs.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
